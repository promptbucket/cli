# PromptBucket CLI Environment Configuration
# Copy this file to .env and customize as needed

# API Configuration
PROMPTBUCKET_BASE_URL=https://harbor.promptbucket.co
PROMPTBUCKET_API_VERSION=v1

# Environment (used for loading environment-specific .env files)
ENVIRONMENT=development

# Debug mode (shows which .env files are loaded)
# DEBUG=true

# Development overrides (use .env.local for these)
# PROMPTBUCKET_BASE_URL=http://localhost:8080